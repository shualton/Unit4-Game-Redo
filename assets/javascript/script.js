$(document).ready(function() {
    var random = [];
    for(var r = 20; r <= 120; r++) {
        random.push(r);
    }
    var powerup = [];
    for(var p = 1; p < 10; p++) {
        powerup.push(p);
    }

    var randNum;
    var powerNum = [];
    var p1;
    var p2;
    var p3;
    var p4;


});